const API_BASE_URL='http://localhost:5000/api';
const api={async request(path,options={}){const token=localStorage.getItem('nexcartToken'),controller=new AbortController(),timer=setTimeout(()=>controller.abort(),2500);try{const response=await fetch(API_BASE_URL+path,{credentials:'include',headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{})},signal:controller.signal,...options});const data=await response.json().catch(()=>({success:false,message:'Server returned an invalid response'}));if(!response.ok)throw new Error(data.message||'Request failed');return data}finally{clearTimeout(timer)}},get(p){return this.request(p)},post(p,b){return this.request(p,{method:'POST',body:JSON.stringify(b)})},put(p,b){return this.request(p,{method:'PUT',body:JSON.stringify(b)})},delete(p){return this.request(p,{method:'DELETE'})}};
