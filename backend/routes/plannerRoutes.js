const router=require('express').Router(),Product=require('../models/Product'),{plan}=require('../services/plannerService');router.post('/recommend',async(req,res,next)=>{try{const budget=+req.body.budget;if(!budget||budget<1)return res.status(400).json({success:false,message:'Enter a valid budget'});const result=plan(await Product.find(),budget,req.body.categories||[],req.body.preferences||[]);res.json({success:true,...result,remaining:budget-result.total,utilization:Math.round(result.total/budget*100)})}catch(e){next(e)}});module.exports=router;
